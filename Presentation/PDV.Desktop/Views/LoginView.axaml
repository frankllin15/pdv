<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:PDV.Desktop.ViewModels"
             x:Class="PDV.Desktop.Views.LoginView"
             x:DataType="vm:LoginViewModel">

    <UserControl.KeyBindings>
        <KeyBinding Gesture="Enter" Command="{Binding LoginCommand}" />
        <KeyBinding Gesture="Escape" Command="{Binding ClearPinCommand}" />
    </UserControl.KeyBindings>

    <Grid Background="#F0F0F0">
        <!-- Centered Login Panel -->
        <Border Background="White"
                CornerRadius="12"
                Padding="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MinWidth="400"
                BoxShadow="0 4 20 0 #40000000">
            <StackPanel Spacing="24">
                <!-- Logo/Title -->
                <StackPanel HorizontalAlignment="Center" Spacing="8">
                    <TextBlock Text="PDV"
                               FontSize="48"
                               FontWeight="Bold"
                               Foreground="#2196F3"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Point of Sale System"
                               FontSize="14"
                               Foreground="#757575"
                               HorizontalAlignment="Center" />
                </StackPanel>

                <!-- Login Form -->
                <StackPanel Spacing="16">
                    <!-- Operator Code -->
                    <StackPanel Spacing="4">
                        <TextBlock Text="Operator Code"
                                   FontSize="14"
                                   Foreground="#424242" />
                        <TextBox Text="{Binding OperatorCode}"
                                 FontSize="18"
                                 Height="48"
                                 Watermark="Enter your code"
                                 IsEnabled="{Binding !IsLoading}" />
                    </StackPanel>

                    <!-- PIN -->
                    <StackPanel Spacing="4">
                        <TextBlock Text="PIN"
                                   FontSize="14"
                                   Foreground="#424242" />
                        <TextBox Text="{Binding Pin}"
                                 FontSize="18"
                                 Height="48"
                                 PasswordChar="*"
                                 Watermark="Enter your PIN"
                                 IsEnabled="{Binding !IsLoading}" />
                    </StackPanel>

                    <!-- Error Message -->
                    <Border Background="#FFEBEE"
                            CornerRadius="4"
                            Padding="12"
                            IsVisible="{Binding HasError}">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#C62828"
                                   FontSize="14"
                                   TextWrapping="Wrap" />
                    </Border>

                    <!-- Login Button -->
                    <Button Command="{Binding LoginCommand}"
                            Height="52"
                            HorizontalAlignment="Stretch"
                            Background="#2196F3"
                            Foreground="White"
                            FontSize="16"
                            FontWeight="SemiBold"
                            IsEnabled="{Binding !IsLoading}"
                            HorizontalContentAlignment="Center">
                        <Panel>
                            <TextBlock Text="Login"
                                       IsVisible="{Binding !IsLoading}"
                                       HorizontalAlignment="Center" />
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8"
                                        IsVisible="{Binding IsLoading}"
                                        HorizontalAlignment="Center">
                                <TextBlock Text="Authenticating..." />
                            </StackPanel>
                        </Panel>
                    </Button>
                </StackPanel>

                <!-- Footer -->
                <TextBlock Text="Enter and press Enter or click Login"
                           FontSize="12"
                           Foreground="#9E9E9E"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Version Info -->
        <TextBlock Text="v1.0.0"
                   FontSize="11"
                   Foreground="#BDBDBD"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Margin="16" />
    </Grid>
</UserControl>
